[project]
name = "technocore-pulse"
version = "1.1.0"
description = "A measuring agent for technocore.chat: throughput, distinct ratio, and a signed receipt archive."
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/erkancamli/technocore-pulse"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
